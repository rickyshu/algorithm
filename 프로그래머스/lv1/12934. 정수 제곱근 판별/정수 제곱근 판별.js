function solution(n) {
            let ans=-1;
          if(parseInt(Math.sqrt(n))===Math.sqrt(n)){
            let num=Math.sqrt(n)
            return (num+1)*(num+1);
          }
          return ans;
}